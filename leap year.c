#include <stdio.h>
int main()
{
	int num,year;
	printf("enter any year");
	scanf("%d",&year);
	if(year%4==0)
		printf("the given year %d is leap year",num);
	else
	    printf("the given year %d is not leap year",num);
}
